{
  "name": "yield-yak",
  "license": "MIT",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ yield-yak",
    "create-local": "graph create --node http://localhost:8020/ yield-yak",
    "remove-local": "graph remove --node http://localhost:8020/ yield-yak",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 yield-yak",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.35.0",
    "@graphprotocol/graph-ts": "0.28.1",
    "i32": "^1.1.0"
  },
  "devDependencies": {
    "matchstick-as": "0.5.0"
  }
}
