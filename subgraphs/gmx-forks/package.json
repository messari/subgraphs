{
  "name": "mummy-finance",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ shashwats22/gmx-arbitrum",
    "test": "graph test",
    "prepare:constants": "mustache protocols/gmx/config/deployments/gmx-arbitrum/configurations.json src/common/constants.mustache > src/common/constants.ts",
    "prepare:yaml": "mustache protocols/gmx/config/deployments/gmx-arbitrum/configurations.json protocols/gmx/config/templates/gmx.template.yaml > subgraph.yaml"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.40.0",
    "@graphprotocol/graph-ts": "0.29.1"
  },
  "devDependencies": {
    "matchstick-as": "0.5.0"
  }
}
