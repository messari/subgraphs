{
  "name": "liquid-river",
  "license": "MIT",
  "scripts": {
    "format": "npx prettier --write .",
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ itsjerryokolo/liquid-river",
    "create-local": "graph create --node http://localhost:8020/ itsjerryokolo/liquid-river",
    "remove-local": "graph remove --node http://localhost:8020/ itsjerryokolo/liquid-river",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 itsjerryokolo/liquid-river"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.21.1",
    "@graphprotocol/graph-ts": "0.20.0",
    "prettier": "2.6.1"
  }
}
