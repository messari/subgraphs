{
  "name": "geist-finance-subgraph",
  "license": "MIT",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ dineshpinto/geist-finance-subgraph",
    "create-local": "graph create --node http://localhost:8020/ dineshpinto/geist-finance-subgraph",
    "remove-local": "graph remove --node http://localhost:8020/ dineshpinto/geist-finance-subgraph",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 dineshpinto/geist-finance-subgraph"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "^0.26.1",
    "@graphprotocol/graph-ts": "0.25.0"
  }
}
