{
  "name": "apeswap",
  "license": "MIT",
  "type": "module",


  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",

    "config": "mustache config/${npm_config_protocol}/${npm_config_network}/${npm_config_network}.json templates/${npm_config_protocol}/${npm_config_protocol}.v2.template.yaml > subgraph.yaml",
    "deploy": "mustache config/${npm_config_protocol}/${npm_config_network}/${npm_config_network}.json config/${npm_config_protocol}/uniswap.v2.template.yaml > subgraph.yaml && yarn codegen && yarn build && yarn deploy --product hosted-service ${npm_config_location}/${npm_config_protocol}-${npm_config_network}",

    "foo": "node --experimental-json-modules deployment/deployment.js"

  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.28.0",
    "@graphprotocol/graph-ts": "0.26.0"
  },
  "devDependencies": {
    "mustache": "^3.1.0"
  }
}
