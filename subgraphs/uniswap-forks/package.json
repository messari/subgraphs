{
  "name": "apeswap",
  "license": "MIT",
  "type": "module",


  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",

    "config": "mustache config/${npm_config_protocol}/${npm_config_network}/${npm_config_network}.json config/${npm_config_protocol}/uniswap.v2.template.yaml > subgraph.yaml",
    "deployment": "mustache config/${npm_config_protocol}/${npm_config_network}/${npm_config_network}.json config/${npm_config_protocol}/${npm_config_template} > subgraph.yaml && yarn codegen && yarn build && graph deploy --product hosted-service ${npm_config_location}",

    "foo": "node --experimental-json-modules deployment/deployment.js"

  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.28.0",
    "@graphprotocol/graph-ts": "0.26.0"
  },
  "devDependencies": {
    "mustache": "^3.1.0"
  }
}
