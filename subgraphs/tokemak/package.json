{
  "name": "tokemak",
  "license": "UNLICENSED",
  "scripts": {
    "format": "npx prettier --write .",
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ tokemak",
    "create-local": "graph create --node http://localhost:8020/ tokemak",
    "remove-local": "graph remove --node http://localhost:8020/ tokemak",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 tokemak"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.26.0",
    "@graphprotocol/graph-ts": "0.24.1"
  },
  "devDependencies": {
    "prettier": "2.6.1"
  }
}
