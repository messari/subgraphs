{
  "name": "synapse-ethereum",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ synapse-ethereum",
    "create-local": "graph create --node http://localhost:8020/ synapse-ethereum",
    "remove-local": "graph remove --node http://localhost:8020/ synapse-ethereum",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 synapse-ethereum"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.32.0",
    "@graphprotocol/graph-ts": "0.27.0"
  }
}
