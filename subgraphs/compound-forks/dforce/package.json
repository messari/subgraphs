{
  "scripts": {
    "prepare:mainnet": "YARN_SILENT=1 yarn mustache config/mainnet.json subgraph.template.yaml > subgraph.yaml",
    "deploy:mainnet": "graph deploy --studio dforce-subgraph-mainnet",
    "prepare:bsc": "YARN_SILENT=1 yarn mustache config/bsc.json subgraph.template.yaml > subgraph.yaml",
    "deploy:bsc": "graph deploy --studio dforce-subgraph-bsc",
    "prepare:arb": "YARN_SILENT=1 yarn mustache config/arbitrum_one.json subgraph.template.yaml > subgraph.yaml",
    "deploy:arb": "graph deploy --studio dforce-subgraph-arbitrum_one",
    "prepare:optimism": "YARN_SILENT=1 yarn mustache config/optimism.json subgraph.template.yaml > subgraph.yaml",
    "deploy:optimism": "graph deploy --studio dforce-subgraph-optimism",
    "prepare:polygon": "YARN_SILENT=1 yarn mustache config/polygon.json subgraph.template.yaml > subgraph.yaml",
    "deploy:polygon": "graph deploy --studio dforce-subgraph-polygon",
    "prepare:avalanche": "YARN_SILENT=1 yarn mustache config/avalanche.json subgraph.template.yaml > subgraph.yaml",
    "deploy:avalanche": "graph deploy --studio dforce-subgraph-avalanche"
  },
  "devDependencies": {
    "@graphprotocol/graph-cli": "^0.30.1",
    "mustache": "^4.2.0",
    "prettier": "^2.6.0"
  }
}
